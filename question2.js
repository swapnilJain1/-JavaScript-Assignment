// 02. Declare variables to store your first name, last name, marital status, country and display them using interploation method.

const first_name = "swapnil"
const last_name = "jain"
const marital_status = "not married"
const country = "India"

console.log(`My name is ${first_name} ${last_name}. I am ${marital_status} and I live in ${country}`)